# mcp-server

**Phase 4.** Model Context Protocol server that exposes an agent's spending capability as structured tool calls (`pay`, `quote`, `balance`, `recent_receipts`). Drop into Claude Desktop, Claude Code, Cursor, or any MCP-compatible host so an LLM can transact within the policy.
