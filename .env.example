# Arc testnet wallet — used for hello-arc connectivity check.
# Fund this address with testnet USDC at https://faucet.circle.com (pick Arc Testnet).
ARC_TEST_ADDRESS=0xYourTestWalletAddressHere

# Optional: override the default RPC. Defaults to https://rpc.testnet.arc.network
# ARC_RPC_URL=
